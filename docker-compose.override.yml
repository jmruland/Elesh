version: '3.8'

services:
  dnd-archivist:
    environment:
      - OLLAMA_API_BASE_URL=http://ollama:11434
    volumes:
      - /mnt/infrastructure/dnd-archivist/lore:/app/lore
      - /mnt/infrastructure/dnd-archivist/prompts/system.txt:/app/system.txt
    ports:
      - "5005:5005"
